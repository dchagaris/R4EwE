% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R_Ecospace_GA_calibration.R
\name{fn.plot_ga_pop_responses}
\alias{fn.plot_ga_pop_responses}
\title{Plot GA population: response-function curves for env/redtide, histograms otherwise.}
\usage{
fn.plot_ga_pop_responses(
  gapop,
  est_par_vec,
  par.groups,
  par.labels,
  env_pars = NULL,
  redtide_pars = NULL,
  file,
  dims = c(3, 3),
  highlight_pars = NULL
)
}
\arguments{
\item{gapop}{Numeric matrix \code{[popSize x n_pars]} of parameter values.}

\item{est_par_vec}{Named numeric vector of baseline parameter values.}

\item{par.groups}{Character vector mapping each column of \code{gapop} to a group.}

\item{par.labels}{Character vector of column labels (typically the colnames of \code{gapop}).}

\item{env_pars}{Data frame of env response baselines (canonical or sensitivity-results
schema, see \code{\link{fn.makeparvec}}). May be \code{NULL}.}

\item{redtide_pars}{Same as \code{env_pars} for red-tide responses. May be \code{NULL}.}

\item{file}{Output PDF path.}

\item{dims}{Panel grid \code{c(rows, cols)}. Default \code{c(3, 3)}.}

\item{highlight_pars}{Numeric vector of length \code{ncol(gapop)} whose values get the
blue highlight (vline on histograms, overlaid curve on env/rt panels). If
\code{NULL} (default), \code{est_par_vec} is used — i.e. the baseline is highlighted,
matching the original behavior used for the initial-population plot. Pass the
min-LL individual (e.g. \code{gapop[which.min(fitness), ]}) for the final-population
posterior plot.}
}
\value{
Invisibly returns \code{file}.
}
\description{
For each unique parameter group, plots a panel:
\itemize{
\item Env response groups (\code{env<N>_type<S>}) and red-tide groups (\code{rt<N>_type<S>}):
overlay one response curve per population member, with a highlighted curve
drawn in blue. By default that curve is the baseline; pass \code{highlight_pars}
to highlight a different parameter set (e.g. the min-LL individual from the
final generation).
\item All other groups (vul, disp, fleetdyn, med): one histogram per parameter with
a vertical dashed line at the highlighted value.
}
Writes the panels to a multipage PDF.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Spatial_likelihood.R
\name{fn.mdat_default_attributes}
\alias{fn.mdat_default_attributes}
\title{Build the default MDAT per-map attributes table.}
\usage{
fn.mdat_default_attributes(crosswalk = NULL, survey_meta = NULL)
}
\arguments{
\item{crosswalk}{see \code{fn.mdat_default_crosswalk}.}

\item{survey_meta}{see \code{fn.mdat_default_survey_meta}.}
}
\value{
data.frame(survey, species, season, use, pcs, months, styear,
endyear, logspace).
}
\description{
One row per (survey x species x season) map, seeded from the
crosswalk (all ages) and survey metadata (seasons, months, year window,
log-space flag). This is the editable control surface: trim \code{pcs} to
the ages a given survey surface actually informs, adjust \code{months}, and
toggle \code{use}. \code{pcs} and \code{months} are comma-separated strings.
NEAMAP rows default to \code{use = FALSE}.
}

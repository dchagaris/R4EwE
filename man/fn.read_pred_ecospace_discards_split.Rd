% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_output.R
\name{fn.read_pred_ecospace_discards_split}
\alias{fn.read_pred_ecospace_discards_split}
\title{Split Ecospace discards into dead, total, and surviving.}
\usage{
fn.read_pred_ecospace_discards_split(
  dir.out,
  timestep,
  regions,
  styear,
  obs.ts,
  fleet.names
)
}
\arguments{
\item{dir.out}{Path or vector of paths to Ecospace run folders.}

\item{timestep}{\code{"annual"} or \code{"monthly"}.}

\item{regions}{Integer vector of region IDs.}

\item{styear}{Calendar start year used to label the year dimension.}

\item{obs.ts}{Output of \code{\link{fn.read_ecosim_timeseries}}, providing the per-fleet
type-11 DiscardMortality series.}

\item{fleet.names}{Character vector indexed by fleet pool code, used to map the
\code{"fleet|group"} column names to fleet pool codes. If \code{NULL}, every fleet
defaults to Dmort = 1 (total discards collapse to dead discards).}
}
\value{
A list with elements \code{dead}, \code{total}, \code{surv} (each
\code{[years, groups, regions, runs]}) and the fleet|group versions
\code{dead_fg}, \code{total_fg}, \code{surv_fg}; or \code{NULL} if Catch/Landings files
are missing.
}
\description{
Derives predicted dead / total / surviving discards from per-region per-fleet-group
Catch and Landings CSVs. Uses \code{DD = Catch - Landings} (dead discards),
\code{DT = DD / Dmort} (total discards, with per-fleet Dmort taken from the type-11
DiscardMortality series in \code{obs.ts}; a fleet with no type-11 entry defaults to
Dmort = 1 so DT = DD), and \code{DS = DT - DD} (surviving discards). Computed at
fleet x group resolution then summed to group level.
}

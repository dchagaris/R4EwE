% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_output.R
\name{fn.gen_slim_copy}
\alias{fn.gen_slim_copy}
\title{Slim-copy a generation of Ecospace GA run folders}
\usage{
fn.gen_slim_copy(
  dir.in,
  dir.out = paste0(dir.in, "_small"),
  groups = c("gag", "red grouper"),
  fleets = NULL,
  map.kinds = c("Biomass", "Catch"),
  keep.cmd = TRUE,
  keep.misc = TRUE,
  overwrite = FALSE,
  ncores = 1L,
  verbose = TRUE
)
}
\arguments{
\item{dir.in}{Character. Directory of run folders (e.g. \code{.../gen30}).}

\item{dir.out}{Character. Output directory. Default \code{paste0(dir.in, "_small")}.}

\item{groups}{Character vector of group name stems (matched against the
\verb{EcospaceMap...-<stem> <stanza>.csv} filename). Default gag + red grouper.}

\item{fleets}{Character vector of exact fleet names for effort maps,
or the string \code{"all"}, or \code{NULL} (default) to drop every effort map.}

\item{map.kinds}{Character vector of spatial map kinds to consider for the
group filter. Default \code{c("Biomass", "Catch")}. Effort is controlled by
\code{fleets}, not this argument.}

\item{keep.cmd}{Logical. Copy \code{cmd.txt} per run. Default TRUE.}

\item{keep.misc}{Logical. Copy any leftover files (e.g. \code{M0_loss_rate.csv})
that are neither annual TS nor spatial maps nor cmd.txt. Default TRUE.}

\item{overwrite}{Logical. If \code{dir.out} already exists non-empty, delete it
before copying. Default FALSE (function errors out).}

\item{ncores}{Integer. Number of worker processes to run the per-subdir
copy in parallel. Default 1 (sequential). On Windows the outer loop is
the bottleneck; setting \code{ncores = 8}-\code{16} typically gives near-linear
speedup until disk saturates.}

\item{verbose}{Logical. Print progress + summary. Default TRUE.}
}
\value{
Invisibly, a list with \code{dir.out}, \code{n_dirs}, \code{n_files_in},
\code{n_files_out}, \code{bytes_in}, \code{bytes_out}, and any \code{warnings} collected
(e.g. fleets requested that no run dir contained).
}
\description{
Copies a directory of Ecospace GA run folders (e.g. \verb{gen30/}) into a sibling
output directory (default \verb{<dir.in>_small}) that keeps only annual timeseries
plus spatial biomass/catch maps for a chosen set of groups, and optionally
effort maps for a chosen set of fleets. Folder and file names are preserved
verbatim so downstream R4EwE readers work unchanged. Files are copied byte-
for-byte (no re-parse).
}
\details{
What gets copied per run folder:
\itemize{
\item \code{Ecospace_Annual_Average_*.csv} — all kept.
\item \code{Ecospace_Average_*.csv} (monthly averages) — dropped.
\item \verb{EcospaceMap(Biomass|Catch)-<group> <stanza>.csv} — kept for groups in \code{groups}.
\item \verb{EcospaceMapEffort-<fleet>.csv} — kept for fleets in \code{fleets} (exact
match on the trailing token, or \code{fleets = "all"} for every effort map;
NULL drops them all).
\item \code{cmd.txt} — kept if \code{keep.cmd}.
\item Anything else (e.g. \code{M0_loss_rate.csv}) — kept if \code{keep.misc}.
}
}

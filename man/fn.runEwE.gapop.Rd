% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R_Ecospace_GA_calibration.R
\name{fn.runEwE.gapop}
\alias{fn.runEwE.gapop}
\title{Run Ecospace in parallel, for GA calibration.}
\usage{
fn.runEwE.gapop(files.cmd, obj.fxn = 1, delete.output = T, max_tries = 5)
}
\arguments{
\item{files.cmd}{Character vector of paths to Ecospace command files.}

\item{obj.fxn}{Integer or flag selecting which objective function to use.}

\item{delete.output}{Logical; if true, delete generated output folders after runs.}

\item{max_tries}{Maximum number of retry rounds for failed runs.}
}
\value{
Numeric vector of fitness values (same length as files.cmd).
}
\description{
Executes Ecospace runs from a vector of command file paths using parallel
workers, retries any failed runs up to a limit, optionally deletes outputs,
and returns the fitness (e.g., log-likelihood) vector.
}
\details{
Uses foreach with a parallel backend to run fn_runEwE on each command file.
Failed runs (NA results) are identified and resubmitted up to max_tries times.
If delete.output is true and run_dir exists in the environment, subdirectories
under run_dir are removed after completion.
}

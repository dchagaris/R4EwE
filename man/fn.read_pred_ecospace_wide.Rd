% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_output.R
\name{fn.read_pred_ecospace_wide}
\alias{fn.read_pred_ecospace_wide}
\title{Read Ecospace per-region prediction arrays.}
\usage{
fn.read_pred_ecospace_wide(
  dir.out,
  varname,
  timestep,
  regions,
  styear,
  aggregate_by_group = FALSE
)
}
\arguments{
\item{dir.out}{Path or vector of paths to Ecospace run folders.}

\item{varname}{Variable to read: typically \code{"Biomass"}, \code{"Catch"}, or \code{"Landings"}.}

\item{timestep}{\code{"annual"} or \code{"monthly"}.}

\item{regions}{Integer vector of region IDs to read.}

\item{styear}{Calendar start year used to label the year dimension.}

\item{aggregate_by_group}{Logical. If TRUE, sum \code{"fleet|group"} columns to group level.}
}
\value{
A 4-D array \code{[years, groups, regions, runs]} or \code{NULL} if no matching files found.
}
\description{
Reads the per-region \code{Ecospace_Annual_Average_Region_<n>_<Var>.csv}
(or \code{Ecospace_Average_Region_*} for monthly) outputs from one or more Ecospace run
folders and packs them into a 4-D array
\code{[years, groups (or fleet|group), regions, runs]}. When \code{aggregate_by_group = TRUE}
any \code{"fleet|group"} columns are summed to the group level.
}

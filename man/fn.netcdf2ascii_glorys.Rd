% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ST_data_extraction.R
\name{fn.netcdf2ascii_glorys}
\alias{fn.netcdf2ascii_glorys}
\title{Create ascii files from GLORYS netcdf}
\usage{
fn.netcdf2ascii_glorys(
  nc.files = list.files(dir.glorys, pattern = ".nc$", full.names = T),
  dir.stdriver = dir.stdriver,
  depth = depth,
  scalePP = TRUE,
  do.vars = NULL,
  make.monthly = TRUE,
  make.static = TRUE,
  make.climatology = TRUE
)
}
\arguments{
\item{nc.files}{A character variable containing the full file paths of the netcdf files to
process.}

\item{dir.stdriver}{Parent directory for spatial temporal drivers.  A folder will be created for
each variable in this directory to save the ascii files.}

\item{depth}{Raster grid of depth, matching the basemap of Ecospacem with land cells NA.  Saved ascii files will have
the same dimensions.}

\item{scalePP}{Logical.  TRUE (default) normalizes primary-production drivers to the mean
of the first year of data.}

\item{do.vars}{Optional character vector of variable-name prefixes to restrict which
variables are processed. \code{NULL} (default) processes all.}

\item{make.monthly}{Create the monthly ST driver ascii file.  Default TRUE.}

\item{make.static}{Also create the static maps for initialization. Default TRUE.}

\item{make.climatology}{Logical.  TRUE (default) creates a 12-month climatology stack for
each variable.}
}
\value{
A series of ascii files saved in dir.stdriver directory.
}
\description{
This function opens a netcdf file, loops through variables and timesteps, and
creates ascii files for input to Ecospace. Includes vertical integration of chl-a and coastline filling.
}
\examples{
# example code:
\dontrun{
files.nc = list.files(dir.glorys, pattern=".nc$", full.names=T)
fn.netcdf2ascii_glorys(nc.files = files.nc, depth=depth.15min,
                      dir.stdriver=file.path(dirname(getwd()),'ST drivers','15min'))}
}
